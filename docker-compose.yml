services:
  signal-dashboard:
    build: .
    ports:
      - "3080:3080"
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3080/"]
      interval: 30s
      timeout: 5s
      retries: 3
